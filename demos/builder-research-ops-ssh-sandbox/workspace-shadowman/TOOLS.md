# TOOLS.md

The main agent is the coordinator.

- Use subagents for specialized work.
- Use `builder` for shell or implementation tasks.
- Use `research` for read-heavy analysis.
- Use `ops` for maintenance-style actions.
